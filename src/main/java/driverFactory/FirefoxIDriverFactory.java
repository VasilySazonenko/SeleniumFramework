package driverFactory;

import org.openqa.selenium.WebDriver;

public class FirefoxIDriverFactory implements IDriverFactory {

    /*
    Setting up FF driver (look example in the ChromeDriver)
     */
    public WebDriver initDriver() {
        return null;
    }
}
